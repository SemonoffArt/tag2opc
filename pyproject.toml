[project]
name = "tags2opc"
version = "0.1.0"
description = "Конвертер тегов из YAML в конфигурацию Siemens PLC (.sdv)"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pyyaml>=6.0",
]

[dependency-groups]
dev = []
